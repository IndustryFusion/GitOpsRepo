environments:
  default:
    values:
    - environment/default.yaml
    - common.gotmpl
    - common.yaml
  production:
    values:
    - environment/production.yaml
    - common.gotmpl
    - common.yaml
---
releases:
- name: mongodb
  labels:
    order: first
    app: mongodb
  chart: ./charts/mongodb
  namespace: {{ .Values.namespace }}
  values:
  - values.yaml.gotmpl
- name: icsdb
  labels:
    order: second
    app: icsdb
  needs:
  - mongodb
  chart: ./charts/icsdb
  namespace: {{ .Values.namespace }}
  values:
  - values.yaml.gotmpl