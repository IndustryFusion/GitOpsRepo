apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: if50-backend-ingress
  namespace: {{ .Release.Namespace }}
  annotations:
    cert-manager.io/cluster-issuer: {{ .Values.certmanager.issuer }}
    {{- if (eq .Values.ingressType "traefik") }}
    traefik.ingress.kubernetes.io/router.middlewares: {{ .Release.Namespace }}-remove-path-if50-backend@kubernetescrd
    {{- else }}
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: $1
    {{- end }}
spec:
  rules:
  - host: {{ .Values.if50.externalHostname }}
    http:
      paths:
      - backend:
          service:
            name: if50-application-service
            port:
              number: {{ .Values.if50.backendPort }}
        {{- if (eq .Values.ingressType "nginx") }}
        path: {{ .Values.if50.backendExternalPath }}(.*)
        {{- else }}
        path: {{ .Values.if50.backendExternalPath }}
        {{- end }}
        pathType: ImplementationSpecific
  tls:
  - hosts:
    - {{ .Values.if50.externalHostname }}
    secretName: {{ .Values.certmanager.secret }}-if50-backend
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: if50-frontend-ingress
  namespace: {{ .Release.Namespace }}
  annotations:
    cert-manager.io/cluster-issuer: {{ .Values.certmanager.issuer }}
    {{- if (eq .Values.ingressType "nginx") }}
    kubernetes.io/ingress.class: nginx
    {{- end }}
spec:
  rules:
  - host: {{ .Values.if50.externalHostname }}
    http:
      paths:
      - backend:
          service:
            name: if50-application-service
            port:
              number: {{ .Values.if50.frontendPort }}
        path: {{ .Values.if50.frontendExternalPath }}(.*)
        pathType: ImplementationSpecific
  tls:
  - hosts:
    - {{ .Values.if50.externalHostname }}
    secretName: prod-cert-tls
---
{{- if (eq .Values.ingressType "traefik") }}
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: remove-path-fleetmanager-backend
spec:
  replacePathRegex:
    regex: ^{{ .Values.fleetmanager.backendExternalPath }}(.*)
    replacement: /$1
{{- end }}