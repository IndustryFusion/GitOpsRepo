apiVersion: v1
kind: Secret
metadata:
  labels:
    app: xana-docs-application-service
  name: xana-docs-basic-auth
  namespace: {{ .Values.xana_docs.Namespace }}
type: Opaque
stringData:
  # Consumed by ingress-nginx via the auth-secret annotation below; the key
  # must be `auth` and the value htpasswd-formatted.
  #
  # `required` is deliberate: without it an unset value would deploy this
  # publicly reachable site with no authentication at all. Failing the deploy
  # is the safer outcome — this documentation is partner-facing, not public.
  # deploy.yaml injects it with:
  #   --set xana_docs.htpasswd='<the XANA_DOCS_HTPASSWD secret>'
  # (single-quoted, because a bcrypt hash contains '$' sequences)
  auth: {{ required "xana_docs.htpasswd must be set - refusing to publish an unauthenticated partner-facing site" .Values.xana_docs.htpasswd | quote }}

---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: xana-docs-application-service
  name: xana-docs-application-service
  namespace: {{ .Values.xana_docs.Namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: xana-docs-application-service
  template:
    metadata:
      labels:
        app: xana-docs-application-service
      annotations:
        # Roll the pods when the nginx config changes. Without this, editing
        # the ConfigMap updates it in place and the running pods keep serving
        # the old config until something else restarts them.
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
    spec:
      containers:
      - name: xana-docs
        # Pinned to ifx40, not the shared .Values.mainRepo (ibn40). ibn40 is a
        # public namespace: the image contains the full documentation, so a
        # public repo would make every page pullable without credentials and
        # defeat the Ingress basic auth below. Same override pattern as
        # ifx_connector.
        image: {{ .Values.xana_docs.mainRepo }}/xana-docs:{{ .Values.xana_docs.mainVersion }}
        imagePullPolicy: Always
        ports:
        - containerPort: {{ .Values.xana_docs.port }}
        # No envFrom here. The site is static and needs no runtime config;
        # referencing a non-existent vault-secrets would wedge the pod in
        # CreateContainerConfigError.
        volumeMounts:
        - name: nginx-config
          mountPath: /etc/nginx/conf.d/default.conf
          subPath: default.conf
        livenessProbe:
          httpGet:
            path: /healthz
            port: {{ .Values.xana_docs.port }}
          initialDelaySeconds: 10
          periodSeconds: 20
          timeoutSeconds: 3
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /healthz
            port: {{ .Values.xana_docs.port }}
          initialDelaySeconds: 3
          periodSeconds: 10
          timeoutSeconds: 3
        resources:
          requests:
            cpu: "10m"
            memory: "32Mi"
          limits:
            cpu: "200m"
            memory: "128Mi"
      volumes:
      - name: nginx-config
        configMap:
          name: xana-docs-nginx-config
      imagePullSecrets:
      - name: {{ .Values.pullSecretCredentials }}

---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: xana-docs-application-service
  name: xana-docs-application-service
  namespace: {{ .Values.xana_docs.Namespace }}
spec:
  selector:
    app: xana-docs-application-service
  ports:
  - name: http
    port: {{ .Values.xana_docs.port }}
    protocol: TCP
    targetPort: {{ .Values.xana_docs.port }}
  type: ClusterIP

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  labels:
    app: xana-docs-application-service
  name: xana-docs-ingress
  namespace: {{ .Values.xana_docs.Namespace }}
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    # Basic auth. The referenced Secret must live in this same namespace.
    #
    # This is what actually protects the documentation. Docsify is client-side
    # and fetches each page as a raw .md file, so the gate has to sit in front
    # of every request rather than in front of index.html — which is exactly
    # what a path-prefix Ingress rule does.
    nginx.ingress.kubernetes.io/auth-type: basic
    nginx.ingress.kubernetes.io/auth-secret: xana-docs-basic-auth
    nginx.ingress.kubernetes.io/auth-realm: {{ .Values.xana_docs.authRealm | quote }}
spec:
  tls:
  - hosts:
    - {{ .Values.xana_docs.host }}
    secretName: xana-docs-tls
  rules:
  - host: {{ .Values.xana_docs.host }}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: xana-docs-application-service
            port:
              number: {{ .Values.xana_docs.port }}
